# =============================================================================
# API Test Environment — copy this file to .env and set your real values.
# Do not commit .env (it is in .gitignore).
# =============================================================================
# The project loads .env from the project root (java-rest-api-automation/)
# when you run: mvn test
# =============================================================================

# --- Required for most API tests ---
# Base URL of the API (no trailing slash). Example: https://api.example.com
BASE_URL=https://api.example.com

# --- Required for auth tests (e.g. endpoint_requiring_auth_returns_200_or_201_with_valid_token) ---
# Bearer token for protected endpoints. Get it from your API (e.g. login endpoint).
AUTH_TOKEN=your_bearer_token_here

# Path of a protected endpoint to call (GET). Example: /api/users or /api/me
PROTECTED_ENDPOINT=/api/protected/resource

# --- Optional: for future tests (list, create, etc.) ---
# LIST_ENDPOINT=/api/posts
# CREATE_ENDPOINT=/api/posts
# Single resource path is often: LIST_ENDPOINT + "/{id}"
