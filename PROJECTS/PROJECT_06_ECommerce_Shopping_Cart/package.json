{
  "name": "project-06-ecommerce-shopping-cart",
  "version": "1.0.0",
  "description": "Playwright TypeScript test suite for e-commerce shopping cart flow",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:flow": "playwright test --grep @flow",
    "test:cart": "playwright test --grep @cart",
    "test:checkout": "playwright test --grep @checkout",
    "test:browse": "playwright test --grep @browse"
  },
  "keywords": [
    "playwright",
    "typescript",
    "test-automation",
    "ecommerce",
    "shopping-cart",
    "e2e-testing"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}

