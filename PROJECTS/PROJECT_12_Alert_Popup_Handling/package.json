{
  "name": "project-12-alert-popup-handling",
  "version": "1.0.0",
  "description": "Playwright TypeScript test suite for alert and popup handling",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:alert": "playwright test --grep @alert",
    "test:confirm": "playwright test --grep @confirm",
    "test:prompt": "playwright test --grep @prompt",
    "test:alert-handling": "playwright test --grep @alert_handling"
  },
  "keywords": [
    "playwright",
    "typescript",
    "test-automation",
    "alert",
    "popup",
    "dialog",
    "e2e-testing"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}

